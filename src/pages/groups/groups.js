import * as React from "react";
import styled from "styled-components";

function Groups() {
  return <Wrapper>Groups</Wrapper>;
}

const Wrapper = styled.div`
  color: var(--color-white);
`;

export default Groups;
